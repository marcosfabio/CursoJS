function soma(n1, n2) {
    return n1+ n2
}

console.log(soma(2, 5))

// os parâmetros podem receber valores padrão para caso não
// seja informado como parâmetro da função. Por exemplo:

/*

function soma(n1=0, n2=0) {
    return n1+ n2
}

*/

// Assim, caso n1, não seja informado o valor 0 será atribuido. Idem para n2.

